// components/FavoriteCounter.js
import React from 'react';

const FavoriteCounter = ({ totalFavorites }) => {
  return <div>Total Favorites: {totalFavorites}</div>;
};

export default FavoriteCounter;
